class Space:
    dim_lens = [0,0,0]

    partitions = []

    get_dim_len(dim):
        return dim_lens[dim]

    get_partitions():
        return parti


def make_partitions(space, size):
    for i in xrange(0, space
    lower_left 
